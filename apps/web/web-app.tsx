import React from 'react';

export function WebApp() {
    return (
        <main style={{ fontFamily: 'Inter, system-ui, sans-serif', margin: '2rem', lineHeight: 1.4 }}>
            <h1>Coalition Web</h1>
            <p>Web workspace build is configured and running.</p>
        </main>
    );
}
