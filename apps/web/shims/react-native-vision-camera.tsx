import React from 'react';
import { View } from 'react-native';

export const Camera = (props: any) => <View {...props} />;

export const useCameraDevice = () => undefined;
export const useCodeScanner = (config: any) => config;
export const useFrameProcessor = () => undefined;
